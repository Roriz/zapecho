{
  "name": "chat-assistant",
  "type": "module",
  "main": "app.js",
  "scripts": {
    "test": "node --test test/**/*.test.js",
    "start": "node app.js",
    "dev": "node migrate.js && nodemon -q --watch . --exec 'npm run start'"
  },
  "dependencies": {
    "@fastify/postgres": "^5.2.2",
    "@fastify/sensible": "^5.6.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.27.0",
    "fastify-plugin": "^4.5.1",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "postgrator": "^7.2.0"
  }
}
